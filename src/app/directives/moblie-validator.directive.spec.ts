import { MoblieValidatorDirective } from './moblie-validator.directive';

describe('MoblieValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MoblieValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
